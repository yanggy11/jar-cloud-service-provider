spring:
  application:
    name: cloud-service-provider
  cloud:
    config:
#      uri: http://192.168.1.100:8887 # config server
#      uri: http://192.168.25.128:8887 # config server
      name: cloud-config # 要读取的配置文件application属性
      profile: ${config.profile:test} # default config profile
      username: yanggy11
      password: ygy591932230
      discovery:
        enabled: true
        service-id: cloud-config
server:
  port: 1991
eureka:
  client:
    serviceUrl:
#      defaultZone: http://192.168.1.100:8761/eureka/
      defaultZone: http://192.168.25.128:8761/eureka/

security:
  basic:
    enabled: false
management:
#  context-path: /${spring.application.name}
  security:
    enabled: false
mybatis:
  mapper-locations: sql-mapper/*.xml